import React from "react";
import ReactDOM from "react-dom/client";
import MySolution from "./MySolution";
import JonasSolution from "./JonasSolution";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <MySolution />
  </React.StrictMode>
);
